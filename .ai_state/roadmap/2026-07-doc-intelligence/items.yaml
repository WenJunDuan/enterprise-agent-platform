# 文档智能 Program items — 2026-07-02 架构评估 + 用户四分叉拍板立项
# status: pending | in_progress | done | blocked
slug: "2026-07-doc-intelligence"
source:
  assessment: "2026-07-02 全仓架构评估（会话交付：tender 无 feature 包 / 三域三套 prompt 机制 / OCR 五能力差距对照）"
  evidence: "logs/s7-model-compare/REPORT.md（底稿32万token>>128K，杠杆在精简底稿）"
  decisions: "roadmap.md 决策记录（混合agent化/结构化检索先行/页级部分结果流/三波次）"

items:
  - id: D1
    slug: eval-tender-scaffold
    stream: infra
    title: "eval_tender.py 正式化 + golden manifest（S7 剩余迁入，全 program 回归闸）"
    effort: M
    wave: 0
    depends_on: []
    status: done # 2026-07-16 D1 SHIP:review PASS(evaluator 4.6/5),7 commit merge 回 main(77d9ffe),main 全量 818 绿+ruff 净,worktree 已清。产出:eval 回归闸(server/tender/eval.py 评分核+score_consistency 跨次极差+运维指标 retry/latency)、评标核心下沉 server/tender/runner.py、OCR 方案 i 单向守卫、TENDER_EVAL_MODEL、golden manifest+runbook。2 P2 defer(float显示→polish/case_dir→CLI例外);ARCHITECTURE 分层图已重绘。一致性硬门锁定(V4Pro 基线后)=D4 前置
    note: >
      复用 server/audit/eval.py 骨架。原 S7 harness(logs/ gitignored)已从磁盘蒸发,方法论存
      compound/2026-07-01-learning-flash-tender-eval-inconsistency.md——按重建+正式化实施。
      design 要点:评标核心 _run_evaluation 下沉 server/tender/runner.py(critic F1,与 audit 同构,
      前置完成 D2 一块);一致性 null 语义复用 tender_output.is_real_number(F2);
      **一致性阈值硬门锁定(V4Pro 基线标定后二次 commit)是 D4 开工前置条件(F4)**。
      **round2 F5(P0)已决议(2026-07-15 用户拍板方案 i):ocr 降为 feature 域之下服务层,
      _run_evaluation 连 ocr_preprocess_block 一起下沉(tender→ocr 合法),TENDER_OCR_PURPOSE
      挪家 server/tender/,T5 守卫改单向(禁 ocr→tender/audit);T2 改口"纯移动为主+两处接缝
      补针对性测试"。详见 design.md Round 2 决议记录 +
      compound/2026-07-15-decision-ocr-service-layer.md。**
      范围含 S7 迁入项：tender_golden_manifest.json（仿 tests/eval_fixtures）、
      tender_worker TENDER_EVAL_MODEL per-call 覆盖
      （仿 _TENDER_EFFORT 先例；get_flash_model_config() 专名已在 design.md 中放弃，泛化为任意模型名覆盖）。用户侧待办挂此：填 MODEL_CONTEXT_WINDOW 真实窗口重测截断防护、
      部署机手跑 Flash vs V4Pro 对比。
    refs: ["旧 roadmap S7 remaining_migrated", "compound/2026-07-01-learning-flash-tender-eval-inconsistency.md"]

  - id: D2
    slug: tender-feature-package
    stream: refactor
    title: "建 server/tender/ feature 包：routes/common 里约 3250 行 tender 逻辑归位"
    effort: L
    wave: 0
    depends_on: []
    status: done # 2026-07-18 merge 260a140:T1-T5 纯移动(worker/compare_worker/doc_pipeline→server/tender/ + tender.py→routes/tender/ 分节包 + features↛routes/ops/api 守卫);review PASS(reviewer CLEAN 逐字节+spec PASS);main 834 绿。范围收窄=纯移动(round2 F6 揭示 schema 分家实为共享 contract 行为重构)→残留 tender_output/evidence_resolution 拆出独立 sprint tender-schema-split(F6+F5),见 compound/2026-07-16-decision-carve-f6-schema-split-from-d2.md
    redzone: server-refactor
    note: >
      纯移动零行为变更，红区 worktree 强制（铁律[零写入]）。迁入：routes/ 的
      tender_worker/tender_compare_worker/tender_doc_pipeline + common/ 的
      tender_output/evidence_resolution；routes/tender.py(912) 拆薄为 HTTP 壳（可按
      tasks/projects/docs 资源拆）。layering test 新增守卫：routes/ 不含 worker/prompt 构建；
      tender 补进分层图与 feature 互斥规则。测试护航：文件名含 tender 的测试 14 个、
      内容引用 tender 的 25 个（2026-07-03 实测修正，原"39 文件"系虚数）。
      **round2 两处阻塞（D1 design.md Round 2 F5/F6）：①已解——2026-07-15 D1 拍板方案 i
      （ocr 降为服务层，tender→ocr 合法），tender_doc_pipeline 迁入不再撞守卫；
      ②tender_output/evidence_resolution 被 common/output_contracts.py:30,460
      （共享契约机，audit 亦消费）import，直接迁入会造 common→feature 逆向依赖——D2 design
      时决定"tender 专属校验一起下沉"或"两模块留 common"。**2026-07-03 深评给出第三解(推荐):
      schema 分家——注册 tender/audit-result.schema.json 专属处理器,tender hooks+evidence
      resolve 挂 tender 名,expense 用纯通用处理器(现状 tender 校验无条件跑在 expense 结果上,
      output_contracts.py:462-469 单 schema 多域共用是根因),分家后 tender_output 可随包合法
      迁移;顺带修 contract.py:252 import-time 副作用注册。范围另+:worker 三胞胎骨架抽
      background job harness(准入闸+引用集+信号量+超时+三态upsert 60-70%同构)、tender.py
      按既有 banner 分节拆 tasks/projects/docs/compare(:133/223/441/627/854 已就位)。
      详见 compound/2026-07-03-explore-arch-deep-review-deltas.md #7/#8/#12。**
    refs: ["2026-07-02 架构评估 P0-1", "D1 design.md Round 2 F5/F6"]

  - id: D3
    slug: prompt-single-source
    stream: refactor
    title: "三域 prompt 投递机制收敛 + .claude 瘦身"
    effort: M
    wave: 0
    depends_on: []
    status: done # 2026-07-18 DONE(merge 13ec8b1+push 304b624):用户拍方向 b(Python 单源)。spike(route-note.md)三模式时延中位差<6%=延迟不卡方向;意外主发现=两源漂移兑换成可靠性差(内联 4/6 漏 explanation vs command 1/7)+prompt-闸矛盾(先修 ship 60d860c)。实施=audit.md 改薄壳(判断纪律单源 runner.py:AUDIT_INSTRUCTIONS,首步强制 Read);review 三件套 PASS+F1-F4 修复+真网关 CC 路径验收 verdict 与 Python 一致。.claude expense 资产已标注非生产真相源
    note: >
      先 spike：实测 expense 走 command 模式（setting_sources=["project"]）的延迟差值，
      与现 AUDIT_INSTRUCTIONS 内联对比 → 差值可接受则统一到 tender 模式（command 文件单一真相源），
      否则反向统一（.claude expense 资产标注非生产真相源）。捎带 .claude 侧收敛：
      CLAUDE.md tender 实施细则下沉 skill/command（只留路由表+红线）、
      默认关闭的 6 个 agent 纪律文本抽 shared reference、skill/command 命名统一 <domain>-<verb>。
    refs: ["2026-07-02 架构评估 P1-3", "compound: 2026-06-12 audit.md 生产不生效教训"]

  - id: D4
    slug: l2-model-routing
    stream: feature
    title: "OCR 分支内 L2 页/区域级多模型路由 + 模型池注册（含印章/手写 POC）"
    effort: L
    wave: 1
    depends_on: [D1]
    status: pending
    note: >
      **2026-07-20 重定义倾向（用户）**：D4 从「进程内 EngineRegistry」→「建**独立 OCR 路由服务**
      （直读+路由+模型池全内聚），本项目 ocr 调用改指向其单一 API（文件字节→结构化底稿带页锚）」。
      更大但更干净；前置不变（V4Pro 基线锁硬门+模型池实测能力画像）；D9 不阻塞（流式 job 端点将来
      改代理 OCR 服务流，契约+前端复用）。硬约束：页锚保真+对齐 doc-structure.schema.json+种子=现
      server/ocr/ 不重写。详见 compound/2026-07-20-decision-ocr-as-standalone-service.md，D4 窗口正式确认。
      ——以下为原进程内方案锚点（若确认拆服务，迁入新 repo 作种子）——
      前置：D1 一致性阈值已锁硬门（警告模式不得跨 Wave 存活，critic F4）。
      **模型池定案（用户 2026-07-02，部署机三模型）**：①PaddleOCR 经典管道=便宜快打底
      （清晰印刷页默认）；②PaddleOCR-VL=复杂版面/表格/低清升级层（现主力）；
      ③Unlimited-OCR（baidu 2026-06，3B/激活500M，R-SWA 一次前向数十页、KV cache 恒定，
      github.com/baidu/Unlimited-OCR）=整本长文档一次性解析层（标书全本→章节树，
      直接服务 D6 结构化+D8 底稿瘦身，SGLang/transformers 部署）。
      路由形态=五级决策梯（用户 2026-07-02 梳理定稿，全文见
      compound/2026-07-02-decision-ocr-routing-ladder.md）：**T0 直读优先显式化**
      （能直读绝不 OCR，classify+native 已有，混合 PDF 页级直读+补OCR 为先例）→
      T1 PaddleOCR 打底→T2 低置信/复杂版面升 VL（extract_pdf_subset 只重跑差页）→
      T2s 印章 seal 产线→T3 整本长程 Unlimited-OCR→**T4 兜底=系统配置文件配置的
      多模态大模型**（MODEL_BASE_URL/MODEL_NAME，config._forward_model_env；当前部署=
      内网 qwen3.6-27b，**设计不 pin 型号，换模型改 .env 不改代码**）。
      期望成本最小化（成本单调递增只升不降），非单模型最优。
      实现锚点：engine.recognize() 的 env if/elif 换 EngineRegistry+能力画像+路由策略；
      cache._engine_fingerprint 升 per-engine 进 cache key；classify(L1 文件类型路由)不动。
      模型池经统一 litellm 网关注册，兼容内网自托管与云网关双部署形态。
      印章/手写 POC（memory 挂账待办）在此落地，三模型能力画像以 POC 实测为准（铁律[出处优先]，
      不凭记忆填基准）。捎带：ocr/runner.py 迁离 core 门面私有名 import（_extract_json_object）。
      改动须过 D1 golden case 回归。
    refs: ["multi-ocr references/engines.md", "references/routing.md", "memory: project_ocr_form_fill 待办", "https://github.com/baidu/Unlimited-OCR", "https://arxiv.org/abs/2606.23050"]

  - id: D5
    slug: ocr-decision-agent
    stream: feature
    title: "OCR 决策点 agent 化（混合形态）：低置信度/异常页自检自纠环"
    effort: M
    wave: 1
    depends_on: [D3, D4]
    status: pending
    note: >
      用户拍板=混合：Python 管道骨架不变，LLM 只在分支介入——低置信度页复核、
      结果自检（表格错位/金额格式异常→换引擎重跑）、失败页自愈。决策点 prompt 按 D3
      收敛后的单源机制管理。不做全 agent 驱动（慢/贵/难回归，与确定性底稿校验链冲突）。
    refs: ["roadmap.md 决策1"]

  - id: D6
    slug: doc-understanding
    stream: feature
    title: "文档级结构化：章节树/目录索引/页锚/跨页表格/关键实体 → 结构化底稿 schema"
    effort: L
    wave: 2
    depends_on: [D1]
    status: done # 2026-07-18 merge c874bd8(提前拉起,与 D2 并行,codex sol-high 实现):server/ocr/docstructure.py 确定性(复用 boq _PAGE_RE/_AMOUNT_STRICT/_PAGE_CARRY_LINES,章节树+语义标签+实体+跨页表格保守合并,页锚硬护栏 page=真实号或 null 不臆造)+doc-structure.schema.json+12测试;主agent通读+独立跑测通过。tender S1 接线(find_chapters_by_tag 消费定位 evaluation_method 章)留薄 follow-up;D8 底稿瘦身可复用
    note: >
      从页级 VL 输出（Markdown+JSON）解析文档级结构：章节树+语义标签（资格审查章/评标办法章/
      业绩章）、跨页表格合并、实体（金额/日期/资质证书号/项目经理名）。boq.py 是领域感知先例。
      产出 .claude/contracts 新 schema（结构化底稿契约）。页锚【第N页】全程保真（硬护栏）。
      直接服务 tender S1 定位 criteria。可与 Wave1 并行起，建议 D4 后收口以吃到识别质量提升。
    refs: ["tender S1 定位需求", "护栏: 页锚保真"]

  - id: D7
    slug: structured-rag
    stream: feature
    title: "结构化 RAG：底稿入库（章节树+页锚+FTS/BM25）+ 检索接口（并入旧 S9 KB+外部数据脚手架）"
    effort: L
    wave: 2
    depends_on: [D6]
    status: done # 2026-07-18 merge 3b686d1(与 A 并行,codex sol-high 实现,主agent审读+独立验+全量 844 绿,MVP):server/ocr/rag.py(章节子树分块+index_document/search)+server/stores/rag_store.py(FTS5 trigram 中文分词+BM25+conn 注入,页锚保真硬护栏,_escape_match_query 防查询注入)+docstructure 2 helper 改名复用(chapter_heading/scan_page_context)+10测试。DEFER 后续:tender S1 接线(消费 search 定位 criteria 章)/旧 S9 KB 合并/外部数据脚手架/向量二期
    note: >
      用户拍板=结构化检索先行：SQLite FTS/BM25 + 章节/条款定位，零新依赖，离线部署零成本；
      向量检索作二期（backlog，触发条件见 roadmap.md）。吸收旧 roadmap S9 全部范围：
      KB 补全 + 外部数据适配脚手架（只建脚手架不接真实接口）。检索结果必须带页锚出处。
    refs: ["旧 roadmap S9（merged）", "roadmap.md 决策2"]

  - id: D8
    slug: transcript-slimming
    stream: feature
    title: "底稿瘦身落地：tender S1/S2 改按需检索喂模型 + S7 harness 复测验证"
    effort: M
    wave: 2
    depends_on: [D2, D7]
    status: done # 2026-07-18 merge be85ec0(codex sol-high,主agent审读+独立验,全量 861 绿,MVP):server/tender/context_slim.py 按 criteria(eligibility_rules→qualification_review/items→evaluation_method)检索招标文件章节(内存 FTS5 复用 D6 docstructure+D7 rag,chunk_id 去重,页锚显式前缀保真,任一查询空/无章节则整档回退不部分丢)+runner.py flag 门控 TENDER_SLIM_CONTEXT(默认关,off=同一 loader 函数对象逐字节不变,现有 test_tender_read_layer 10 测未改全过)。**部署机 runbook(codex 范围外,待跑)**:TENDER_SLIM_CONTEXT=1 用真标书跑 S7 harness 对比成本/时延/一致性跨度/policy_refs 四指标,达标再改默认值 1(另开任务)。DEFER:投标(S2)侧检索精简=backlog
    note: >
      tender_worker（已迁 server/tender/）从 32 万 token 全量灌改为按 criteria 项检索相关章节喂模型。
      验收=复跑 logs/s7-model-compare harness 对比：成本、时延、评分一致性跨度、policy_refs 合规率
      四指标均不劣化且成本显著下降。这是全 program 的核心收益点。
    refs: ["S7 压测结论", "D1 回归闸"]

  - id: D9
    slug: streaming-ocr
    stream: feature
    title: "页级流式 OCR：边识别边推每页完成事件+部分结果，前端渐进渲染"
    effort: M
    wave: 3
    depends_on: [] # 原 [D4]:2026-07-20 判为执行序软依赖并解除(streaming 接缝在 pipeline/routes 层,engine 选型在 engine.recognize() 内部,正交;用户拍板 D9 先行),核验见 sprints/2026-07-20-streaming-ocr/route-note.md
    status: in_progress # 2026-07-20 代码全交付+review PASS+polish 完成,已 merge+push origin(至 3d80836)。design 两轮 critic→impl T1-T5(后端 952+前端 121 绿)→review pass1 REWORK(P0 F1 流式回退重复/过期页级单元)→修 176e91c→pass2 PASS(955 passed)→polish(2 P2 清/defer)。**唯一剩 runtime-verify**:用户 mac mini 亲自部署实跑 /ocr/jobs 流式+前端点击流,codex 规整日志回传作实跑证据→主 agent 收口 runtime-verify.md+ship 契约。sprint 2026-07-20-streaming-ocr
    redzone: agent-front
    note: >
      用户拍板=页级部分结果流。后端：pipeline 改 async generator 页级 yield，复用平台 SSE
      进度流机制；前端渐进渲染属 agent-front 红区（需明确授权+worktree）。
      识别-评标流水线重叠作流式二期（backlog）。
    refs: ["roadmap.md 决策3"]

  - id: D10
    slug: expense-hotpath-reliability
    stream: feature
    title: "expense 审核链路可靠性包：热路径直连 spike + 多模态附件预嵌 + 耗时指标 + 休眠资产 runbook"
    effort: M
    wave: 1
    depends_on: [D3]
    status: in_progress # 2026-07-18 ①③④ DONE(merge 13ec8b1+push):①直连 server/audit/direct.py(AUDIT_DIRECT_CONNECT 门控默认关,trust_env=False 防代理坑,凭据分传,传输类回落/契约类不回落,归档接缝);③耗时/token 指标落 AgentRunMeta;④休眠资产 runbook。真网关四项验收 PASS(直连 16.7s vs CLI 52.8s=×32%,verdict 一致)。**②多模态附件预嵌=降级 backlog**(当前网关 deepseek-v4-flash 不支持 vision,POC 两次空回答),待部署机换 vision 模型后启;⑤热路径直连即①本身。剩 ② 未完故 in_progress
    note: >
      吸收 2026-06-12 quality backlog 账外剩余（③④⑤残），expense 域自此全部在册。
      ①热路径直连 spike：官方 anthropic SDK messages.create 直打网关，绕开 claude-agent-sdk
      CLI 子进程（当时定位的最大 flaky 源；2026-06-02 审核 0 工具调用静默编造 incident 亦是动机）。
      **与 D3 强耦合故 depends_on**：D3 spike 若拍板 expense 统一 command 模式（依赖 CLI），
      直连方案需重估；若直连胜出则 expense prompt 保持 Python 单源——二者只活一个，
      D3 出数后同场拍板。②多模态附件预嵌（依赖①形态）+ 耗时分解指标（可离线先做）。
      ③休眠资产重启 runbook（AUDIT_* 配置收敛已 ship，runbook 未做）。
      验收：server/audit/eval.py golden case 不回退（部署机带网关跑 manifest）。
      复核重开（expense-reviewer + post-write hook）为业务开关非工程项，重开条件已录
      .claude/CLAUDE.md，不进本包。
    refs: ["memory: project-quality-backlog(已归档,剩余项在此落账)", "memory: project-audit-tooluse-incident"]

  - id: D11
    slug: tender-discipline-residuals
    stream: feature
    title: "tender 判分纪律残留收口包：R4 安全硬化 + F04 evidence_chain 派生 + glm 技术参数 + R5/R6/R7"
    effort: M
    wave: 1
    depends_on: [D1, D2]
    status: done # 2026-07-19: Batch A(R4 ocr-page 安全接线)+B(F04/R5/R6)+C2/R7(前端 null guard FE-01/02/03,闭合报告500潜根因) 全 merge 回 main(A=7d237ab/B=79b44a1/R7=412c583;codex 并行实现+主agent review。A/B 三件套 PASS 评 4.55/4.7,linchpin bypassPermissions deny 真网关证实;R7 主agent review+独立验 eslint净/build过/test绿)。**C1/glm DESCOPED**(用户 2026-07-19:内网隔离部署 DeepSeek/qwen 不用 glm)。串标围标识别/S8 不进本包。已 push origin(A+B=3bb9119;R7 随本轮)
    redzone: partial-agent-front
    note: >
      收口 2026-06-23 judgment-discipline sprint 六项账外残留（细节见该 sprint design.md）。
      ①R4 #8a ocr-page 重识别 wiring——**安全敏感独立硬化轮**：agent Bash + 可注入 PDF = RCE 面，
      须 can_use_tool 白名单 + 对抗验证（security-checklist P0 命令注入条目）。
      ②F04 evidence_chain 顶层空→服务端从 award_hits 派生（纯服务端，低风险）。
      ③glm 技术参数仍 manual（prompt/model 层；闸只救不造，见
      compound/2026-06-23-learning-gate-rescues-not-creates.md）。④R5 schema 修订。
      ⑤R6 config 修订。⑥R7 前端 null guard（**agent-front 红区**，需授权+worktree，
      可与 D9 前端部分同批授权）。依赖 D2：残留修改落位 server/tender/ 新结构后做，避免二次搬迁；
      D1 回归闸护航。串标围标识别（产品级扩展）与 S8（用户推迟）不进本包。
    refs: ["sprints/2026-06-23-tender-judgment-discipline/design.md", "compound/2026-06-23-learning-gate-rescues-not-creates.md"]

backlog:
  - "向量检索增强（bge-m3 自托管+离线打包）——结构化检索命中率不足或需历史案例语义检索时触发"
  - "识别-评标流水线重叠（流式二期）"
  - "config.py(491)/cli.py(527) 拆分；session store list_conversation_summaries 三处重复（2026-06-12 债）"
  - "手写签名真伪验证（YOLO+孪生网络，非 OCR 域）"
  - "S8 数据安全（用户推迟，拍板后插入，不阻塞本 program）"

# 2026-07-18 执行序重排(D3 spike 出数后, 详 roadmap.md「执行序重排」节): E1=D10①直连 spike(本机,下一步)
# → E2=D3+D10 同场拍板→D3 实施→D10 剩余 → E3=D11(窗口并行,依赖已满足) → E4=chore dependabot
# → 部署机窗口(用户: D8 runbook + V4Pro 基线锁一致性硬门=D4 前置) → E5=D4 → E6=D5 → E7=D9
execution_order: ["D10-spike", "D3+D10-decision", "D3-impl", "D10-rest", "D11", "chore-dependabot", "deploy-window(D8-runbook + D4-prereq)", "D4", "D5", "D9"]

decisions_made:
  - "✅ 2026-07-15 **D1/D2 共用：OCR 分层归属=方案 i**——ocr 降为 feature 之下服务层,守卫改单向(允许 tender/audit→ocr,禁止 ocr→tender/audit)。见 D1 design.md Round 2 决议记录 + compound/2026-07-15-decision-ocr-service-layer.md"
  - "✅ 2026-07-16 **D2 F6:tender_output/evidence_resolution 归属=方案 A schema 分家**——tender 6 校验+evidence resolve 挂 tender 专属 schema 名,audit/expense 用纯通用处理器,两模块合法迁入 server/tender/;行为接缝(schema 名变更)由 D1 eval 闸+接缝测试护航。见 sprints/2026-07-16-tender-feature-package/design.md F6 节 + compound/2026-07-16-decision-schema-split-tender.md"
  - "✅ 2026-07-16 **D2 F1(critic P0):schema 分家机制=schema_path 别名**——SchemaProcessor+register_schema_processor 加可选 schema_path 字段,tender 挂专属 key 但复用 audit-result.json 物理文件(不建新文件/无漂移);apply_schema_semantics 用 processor.schema_path or schema_name 解析物理文件。见 design.md Round1 修订应答"
  - "✅ 2026-07-16 **D2 F5(critic P1):evidence_resolution=拆分(通用留 common)**——通用检索原语(parse_corpus/CorpusIndex/existence_ratio/normalize_text 等 :112-453)留 common(建议独立 server/common/corpus.py 为 D7 预留),仅 resolve_audit_evidence 整合层+scoring 专属助手迁 server/tender/evidence.py。ocr 测试不耦合 tender。见 design.md Round1 修订应答"
  - "✅ 2026-07-18 **prompt-闸矛盾修复=prompt 从严不松闸**(用户拍板,opus4.8 generator worktree 实施,fix 07f1dc8 merge 60d860c):AUDIT_INSTRUCTIONS 数据真实性拒绝改为须引本地反虚报类规则 rule_id(不硬编码,部署解耦),无适用规则降 manual_review;输出纪律显式列六必填字段治 explanation 缺失。闸零改动。真网关验收见 sprints/2026-07-18-prompt-single-source/spike/verify-results.jsonl"
  - "✅ 2026-07-18 **执行序重排(fable5)**:D10①直连 spike 前置为下一步(D3 拍板最后输入),D11 提升为窗口并行项(依赖 D1/D2 已满足),D4 保持部署机前置卡位,dependabot 插 chore 位。详 roadmap.md「执行序重排」节"
  - "✅ 2026-07-20 **D9 先行 + agent-front 红区授权**(用户拍板):部署机窗口(D8 runbook/D4 前置/D10② vision)交 codex 执行;D9 depends_on D4 判为执行序软依赖并解除(代码核验正交,见 sprints/2026-07-20-streaming-ocr/route-note.md);前端渐进渲染红区 worktree 授权随立项生效"

decisions_needed:
  - "D3: 延迟差值可接受阈值（spike 出数后拍板统一方向）"
  - "D10: 直连 vs command 模式二选一（与 D3 同场拍板）+ 直连落地形态（dormant flag-off 现在建 vs 部署机带网关迭代）"
  - "D8: 复测通过标准量化（一致性跨度上限/成本下降目标，design 时敲定）"
  - "D9 传输形态细节: design Round 1 选 A(任务化+轮询,SSE 否决留二期),critic 后如有异议再升"
  - "向量二期触发时机（依赖 D7 上线后的检索命中率数据）"
