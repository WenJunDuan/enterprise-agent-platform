# 生产检验 — 结论义务提示词首次实测（2026-08-20）

> **匿名化纪律**：本档一律角色代号 + 页锚 + 类别枚举，不出现真实机构 / 人名 / 证号 / 项目名
> （同 `eval/golden/` 规范；K0 教训不自犯）。
> **被检对象**：部署机 `agent-backend:0818b4` 上「项目P / 任务R」一单
> （提交 2026-08-19T12:05:35Z，完成 12:13:41Z）。

## 一、对账订正（先于结论）

handoff 三节写的「下会话第一件事 = 核任务 `d82aca89`（项目 `tp-7bbe179ba9fe4161`）」
**该任务与该项目在部署机上均不存在**（线上实存 4 项目 / 6 任务，逐条核对）。
实际可核的是同案另一单（本档「任务R」），也正是 v2.2 拿去做双跑对照的那份报告
（v2.2 头部标注的 `TR-20260819-tp-27fea`，项目号前缀对得上）。

**这是 `compound/2026-08-19-learning-handoff-claims-need-artifact-proof.md` 记录的同一失败
形态第二次发生**（上次是"抢救件从未存在"）：handoff 写下未经产物核验的 ID。
→ 教训档补一条实例，见本档五节。

## 二、检验结论：**提示词不改**（义务已兑现到手段允许的极限）

| 裁决项（v2.1 三节） | 实测 | 判定 |
|---|---|---|
| ④ basic_info 块（招标三要素/投标三要素/应标一致性） | 全出，`consistency` 两条均 `match`，逐条带出处 | ✅ **通过** |
| ① 禁裸 manual_review（三选一 + 三段义务） | 9 评分项中 7 项 manual，**basis 全非空**（65–168 字，均带页锚与已核内容） | ✅ **通过**（"已找到什么/缺什么"在；**"查过哪里"缺**，见下） |
| ① 报价必须提取金额 + 大小写勾稽 | `bid_price.amount = null`；全份结论中「大写」出现 **0 次** | ❌ **未兑现**（根因非提示词，见三节） |
| ② S1 罗列 → S3 逐条销项四段 | criteria 9 项 + eligibility 10 条全逐条销项，四段齐 | ✅ **通过** |

**判词**：结论义务在**手段允许的范围内已全部兑现**，未兑现项的根因全部落在
底稿完整性与工具面，**不在指令层**——这与 v2.2 §〇 的核心裁决（"指令层已对，
差距在运行时手段"）由生产数据独立互证。**故不动提示词**（省下 34 B 预算余量）。

## 三、四条一手证据（均为 v2.2 未掌握或判错的服务端事实）

1. **`criteria` 抽取产出占位项名「评分点2」**（`tag=scored, max=9, mode=manual`）——
   即 v2.2 列D（office 摄取损坏评分表序号 2 行）的**服务端一手实证**：损坏不止让模型读不到，
   它已经**固化进了本次会话规则**。连带解释回归闸「客观分 0/3」里"匹配器未匹配"那一半：
   度量侧 `item_class` 关键词族（业绩→"业绩"）永远匹配不上名为「评分点2」的项。
   → **K2 优先级坐实**；K8 的匹配器分列亦获实证支撑。
2. **v2.2 对照表「应标一致性 · 未单列」一行判错**：结论里单列了且判 `match`
   （双侧项目名 + 编号，附笔误说明）。v2.2 该行的"差异根因"随之作废，
   `facts_precheck` 双保险结论不变。
3. **项内混合形态（v2.2 未提，新）**：「技术参数指标」是 `max=25` 的**合并项**
   （常规 4 + 性能 21）。basis 里模型明确写出常规部分可判（偏离表空白盖章＝无正偏离），
   但因性能部分不可判，**整项 25 分被拖成 manual**——"有据判 0"义务被项内混合绑架。
   这是结构性缺陷，不是模型不肯判。→ 记 proposals（本批不改）。
4. **页锚系统性不可靠**：`evidence_resolution` = checked 25 / resolved 24 / weak 1 /
   **unresolved 0**，但 `page_mismatch_detected 7 / page_unverified 7 / page_corrected 0`
   ——**引文全都在，页号一个都对不上也一个都没纠正**。且结论把「开标一览表」引到 P11，
   而 v2.2 试跑实测其在 P5。→ 与 K3 混合页、K6 刀②（否定断言反证）同源，
   但**页号偏移本身是第五种形态**，四列归因表未覆盖。记 proposals 待 Step 5 定性。

## 四、两个计划级数字（直接影响 Step 5 验收线）

1. **墙钟 486 s = 等待 111 s + 实跑 375 s**。附录B 基线 299 s 是 **Phase A 之前**
   （0818b3 三跑中位）测的。Phase A 的结论义务把单发输出显著加长 → 若沿用 299 s 作对照，
   **agency 臂会替 Phase A 背账**，v2.1「墙钟 ≤120% 对照」验收线失去意义。
   → **Step 5 开跑前必须重测对照臂基线（post-Phase-A，同 n=3）**，否则该指标作废。
2. **结论体量 33,482 字 / 65,262 字节**（0818b3 为 27,716 字 / 53,310 字节，**+21%**）。
   按已裁决的**「字」口径**：33.5 K < 40 K，**不触发 P0.6 复议**；但两轮趋势逼近，
   Step 5 需继续盯该列（连续两轮 >40 K 字才触发）。
3. 附带确证：任务记录**无 `tool_call_count` 字段** → K5 未落，回归闸该列恒 n/a（读侧已就绪）。

## 五、落到执行队列的改动

- **不修提示词**（本档二节判词）→ T1 之后直接进 T2。
- **新增 Step 5 前置件**：对照臂基线 post-Phase-A 重测（四节①）。挂 Step 5 开跑前，
  与 K5（tool_call 计数）同批，否则重测的基线又缺该列。
- **proposals 新增两条**：项内混合项拖累（三节③）、页号偏移第五形态（三节④）。
- **compound 教训档补实例**：handoff ID 未经产物核验（一节），第二次发生。
