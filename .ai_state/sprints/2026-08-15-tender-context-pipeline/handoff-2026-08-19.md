# Handoff — 2026-08-19（下会话唯一入口）

> 接替 `handoff-2026-08-18-night.md`（其对账三节含 08-19 订正）。
> **效力链**：v2 + v2.1 + **v2.2 行为移植令**（`.ai_state/claude/`，收口时用户新落，
> 冲突处以 v2.2 为准，下会话必读——其结论：指令层已对，差距在运行时手段
> 【工具面/补证轮次/vision 进判定流】，与本档三节队列高度重合但以其裁决为准）
> → `plan-2026-08-18-v2-execution.md`。用户四条产品裁决已收编进 v2.1 三节。

## 一、一句话状态

v2 六步的 0/1/2/3 + v2.1 第一批四刀全部完成并上线（`agent-backend:0818b4` +
`agent-front:0819b1`）；收单等就绪/结论义务/语料落盘/facts_precheck/vision-page 工具
全在生产；会话结束时用户一单实跑正在进行（全链路日志绿），**下会话第一件事 = 核它的结论**。

## 二、本日完成清单（全部推送 main=2536b5e 起，勿重做）

1. **Step 1** 部署 0818b3（smoke 含边界如实记档）
2. **Step 2** 基线回填附录 B，Phase 0 过闸（`782e7d4`）：墙钟 299s ✅ / manual 6 /
   召回 2/7（D1 三跑全中——v2"按项检索必漏 D1"被证伪）/ 客观分 0/3 / 勾稽 0/3；
   三跑逐条一致=漏检结构性
3. **Step 3** case-2/3 金标准（merge `58bf547`）：页锚双路互证；素材两处证伪并订正
   （5ccbb361 抢救件从未存在→服务器直拉补齐；53f94fd0 属另一项目→D2 实证改挂）；
   教训落 `compound/2026-08-19-learning-handoff-claims-need-artifact-proof.md`
4. **提交闸改收单等就绪**（`b7f66cf`）：解析中收单、任务等就绪自动开跑（上限=既有派生
   600s 档）、失败/超时明确报错；顺带根治 prewarm_oracle 挂死（25 用例回大部队，
   回归命令不再 --ignore）
5. **Phase A 结论义务**（`5ef7d18`）：四裁决入提示词（净 +771B）、corpus_materialize、
   TENDER_AGENCY 开关、仲裁表零改动机械复核
6. **v2.1 第一批四刀**：前端文案透传+等待感知（`1df7407`，另抓到"提示永不显示"潜伏缺陷）/
   agency 可见性复制式汇集+facts_precheck（`de54b4b`，跨投标人隔离五专测）/
   vision-page（`ea1e5e7`，提示词净 +3B，hook 八类注入探针）/
   度量修正（`d9286d8`，归因 23 条、同义词带出处、补证调用数+结论字节数两列）
7. **隐私三笔**：前端真名清理（`7ae6174`）；两份真名档撤出追踪+gitignore（`000761a`，
   系昨晚 5ea30c2 违令误入库）；纠偏令文件名修复（`b061f8a`）
8. **部署**：0818b4（含 qpdf）+ 前端 0819b1 双镜像上线，smoke 全过；
   回滚位 0818b3 / 0815b3
9. **实跑验证 ×2**：①item_max_invalid 一单——确诊为抽取随机性（同文件 11:38 坏骰
   11:41 好骰），催生"抽取自动重试"刀（见 proposals）；②收单等就绪全链路绿
   （等待 81s → 就绪后 1s 自动开跑，corpus 双侧落盘，unresolved=0），
   任务 `d82aca89`（项目 `tp-7bbe179ba9fe4161`）会话结束时在跑

## 三、下会话执行队列（按序）

1. **核 `d82aca89` 结论**：basic_info 块（招标项目/投标公司/应标一致性）、报价提取+
   大小写勾稽、有无裸 manual_review——结论义务提示词的第一次生产检验，不合格就修提示词
2. **criteria 抽取自动重试刀**（proposals 新 P1）：结构校验不过→错误喂回重抽（上限 2）、
   失败文案分家（结构非法≠扫描件）、严格数字字符串归一
3. **tool_call 计数进任务记录**（proposals P1，Step 5 报告出数前置，落点 worker/tasks）
4. **Step 4 ④b**：D1 摄取实验（部署机容器 p85-90 证书页，裁决路由：摄取期回填 vs
   判定期问答）/ D2 本地重建索引 + 四项机械缺陷修复（授权范围禁令照旧）
5. **vision 三点冒烟**（卡 VISION_PAGE_URL）→ **Step 5 agency 对照**（先登记归因表，
   case-zj-live + case-2 各 ×3 两臂，~$15-20，**按 v2.1 列A验收**：召回≥4/7、
   勾稽 3/3、manual≤3、墙钟≤120%）→ **Step 6 数字裁决**

## 四、待用户裁决（三件，未催）

1. **git 历史重写**：两份真名档内容仍在 5ea30c2..000761a 历史里（GitHub 已推送）。
   a) filter-repo/BFG + force push 抹净；b) 私有仓接受残留
2. **VISION_PAGE_URL / MODEL**：需 OpenAI 兼容多模态 chat 端点（现役 OCR_VL 是
   异步 job API 不能复用）；不配则 vision 冒烟与列B修复全卡
3. **40K 口径**：P0.6 复议线按"字"还是"字节"（v2.1 三节 vs 五节自相矛盾；
   实测 0818b3 结论 27,716 字 / 53,310 字节，两读法结论相反）

## 四之二、用户三裁决落定（2026-08-19 晚 · 本节闭掉第四节，勿再催）

1. **40K 口径 = 按"字"**：27,716 < 40,000，P0.6 复议不触发；"结论字节数"列照常记录，
   触发句按 v2.1 三节原文"40K 字"解释。
2. **git 历史重写 = filter-repo 抹净**：执行序 = ①先把冻结 worktree
   `agent-aceea5e2cd5e05986` 的改动导出 patch 备档（兼作 Phase 2 对新 main 重对齐的底稿，
   filter-repo 与 force push 会使该 worktree 基提交失效）→ ②filter-repo 按两份真名档路径
   抹全历史 → ③`ATHENA_ALLOW_PUSH=1` force push → ④本地 clone 重整。重写后 08-18/19 起
   提交 hash 全变，本档"本日完成清单"的 hash 以重写后映射为准（重写时在本档补一行映射注）。
3. **VISION_PAGE_URL = 暂缺，后配 qwen VL**：用户暂无视觉模型，计划后配 qwen（必须 VL
   型号，如 Qwen3-VL-30B-A3B / Qwen2.5-VL-32B，vLLM 起 OpenAI 兼容 chat 端点即可对上
   VISION_PAGE_URL/MODEL；纯文本 Qwen3 不带视觉，不可用）。vision 三点冒烟与列B/列C
   实效验证移为**支线等端点**；Step 5 不等 vision，列B 按 v2.1 验收线"不变不计失败"记账。

执行队列以 **v2.2 六节开工指令**为准合流（详见本档三节与 v2.2 五节白名单）：
核 d82aca89 → 历史抹净 → case-4 落盘 → office native → 混合页 manifest →
Step 5 前置件（重试刀/tool_call 计数/attempted 契约/D2 四修/示教）→ Step 5 → Step 6；
vision 支线等 qwen VL 端点。

## 五、挂起与债（详见 sprint proposals.md）

- P0.6 冻结 worktree `agent-aceea5e2cd5e05986` **勿 prune**（其改的 tender-evaluate.md/
  runner.py 今日已被 Phase A 动过，Phase 2 启用前须对新 main 重对齐）
- 提示词预算 39,966/40,000（余 34B）——再加句必先删等量
- 行数债：eval/regression.py 1,134 / worker.py 510 / agent_bridge.py 690 / pipeline.py 884
- legacy/inline 路径 agency 空 corpus 承诺（生产 doc-layer 不受影响）
- criteria 抽取心跳缺失（>~300s 误判僵尸）
- **sprint 级门禁债**：design 改后未重新 review（ship 前必须三件套重跑）、
  runtime-verify 档案未落、polish 未做——delivery-gate 会 block

## 六、环境备忘（增量）

- 线上 `agent-backend:0818b4` + `agent-front:0819b1`；deepseek；提交闸新语义生效
- 全量回归命令**不再** `--ignore=tests/test_tender_prewarm_oracle.py`；
  主 checkout 基线 **17F / 1,965P**（17 条全为 OCR 可选依赖既有失败，逐名稳定）
- 本机到部署机：Tailscale 类隧道挡 ICMP，通断用 `nc -z 100.91.100.13 22` 验，勿信 ping
- 本机 shell 有 SOCKS 代理 env，打部署机 HTTP 必须 `env -u ALL_PROXY -u HTTP_PROXY
  -u HTTPS_PROXY ...`（httpx 缺 socksio 直接炸）
- 评标结论字段：P0.5 汇总在 `extracted_data` 顶层（total_score/total_max/
  pending_count/pending_max），不是 score_summary
