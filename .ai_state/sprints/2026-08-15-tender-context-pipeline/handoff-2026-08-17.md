# Handoff — 2026-08-17 会话收尾（下会话按此执行）

> 本档由会话末复盘产出：本日改动的逐项处置结论 + 用户纪律 + 下会话执行清单。
> 背景数据见 `evidence/dry-run-2026-08-17.md`（第四~六轮）与 `design.md` 增量 S5/S6。

## 一、状态快照

- **已推送 main**：`4802fee`（S5 三缺陷修复）+ `b9952e6`/`0746333`（状态档）。全量 17 failed
  / 1614 passed，与基线逐条同名（本机缺 PIL/paddleocr 的既有环境失败），新增 0。
- **本次收尾提交（WIP）**：S6 部分重合回退（`rag.search_overlap` + `_search_item` 两轮）、
  design 增量 S6、度量脚本通用化（`--criteria` 运行时传入）、evidence 第六轮。
  证据层子集 138 测试绿 + ruff 净；**全量回归未跑**（S5 那轮跑过，本增量改动面小，下会话补跑）。
- 生产现役 = 仅第一波（0817b1）。第二波 + S5 + S6 全部未部署。

## 二、本日改动处置表（复盘结论，下会话 review 以此为出发点）

| 改动 | 依赖 | 处置 |
|---|---|---|
| 证伪「检索命中错误证据」旧阻塞项（第四轮） | — | 成立，KD2 不需推翻 |
| 索引去重 + rowid=文档顺序 + 空块过滤 | 无（通用） | **保留** |
| 续接机制（命中后按文档顺序取正文、按每项额度收满） | "正文在标题后"这一普遍事实 | **保留**（核心价值：只有标题=没有原文） |
| 部分重合回退**机制**（查询串自身 6 字窗口，零命中才触发，留痕） | 无（不引入新词） | **保留**；N=6 定位为语言级默认，样本仅验证 |
| `_DECIMAL_HEADING_RE`（强制尾点）+ 切片标签重推 | **单份标书排版** | ⚠️ 样本拟合。标签不在功能规格里（规格=原文+页码，页锚是真的）；但该正则**同时充当续接边界**，不能裸删——见开放决策① |
| 续接边界 = 同族同级下一小节（`heading_rank`） | **章节编号排版** | ⚠️ 同上；编号风格不被识别时续接静默不发生（证据变薄但不出错，**静默**是问题） |
| 把某项目 9 个评分项写死进度量脚本 | — | ❌ 已改：`--criteria` 运行时传入，仓库零项目数据 |
| 抢位守卫测试（未红即绿） | — | ❌ 已删（防"已修"假象） |

## 三、用户纪律（本日四次纠偏，长期有效）

1. **样本只用于验证，不得当规格**——不许照 1 份或 3 份文档标定阈值/正则再写成产品常量。
2. **任何具体项目的评分项/数据不得进仓库**（脚本、提示词、常量都算）。
3. **判断类能力放 `.claude` skills/agents（Claude Agent SDK）侧**；Python 限于文档物理层
   （OCR/页锚）、任务生命周期、契约门禁、**有界原语**（先例：`ocr-page`）。
4. **不推翻能跑的东西**——这是功能优化，处置按增量走，别拿"全删重来"当纠错。

## 四、下会话执行清单（按序）

1. **补跑全量回归**（S6 增量的 NO_NEW_FAILURES 收口；期望仍是那 17 条环境失败）。
2. **修招标抢位**（通用缺陷，evidence 第六轮有数据）：投标层优先，招标层仅 basis 指向或投标零命中
   时参与；先造**真红**测试（小语料要让招标块 BM25 真的压过投标块），保住
   `test_basis_指向招标章节的项带出招标chunk`。
3. **开放决策①——续接边界去排版化**（小设计增量 + 简评审）：候选 A=停在「下一个被任何评分项
   命中的块」（各项命中点天然分段，零排版假设，可顺带删 `heading_rank`/`_DECIMAL_HEADING_RE`）；
   候选 B=维持现状 + 识别失败时**显式留痕**（warning，不静默变薄）。定了再动代码。
4. **开放决策②——KD8：`search-bid` 有界检索工具**（判断回模型侧，`ocr-page` 形态：单次 ≤N 块、
   带页锚、次数封顶计入 KD3 账）。这**推翻 KD3b 已评审结论**（其出处是 08-14 反复 Read 事故），
   必须走 design 增量 + critic 评审，不得直接实施；10 分钟目标随之重测。
5. **athena-review 整体 review**（reviewer + spec-compliance 并行 → evaluator VERDICT），
   范围 = `aa08c4e..HEAD` 全部第二波+S5+S6；用户点名 **Fable 5 执行并实跑**。
6. **实跑**：`server/tender/eval.py` golden-case + 度量脚本（criteria 运行时构造）；部署机窗口
   做 E2E 计时（注入 3,480→23,594 字后 10 分钟目标未验，超时按满分权重分配每项额度，不回退取量）。
7. 全部收口后再谈部署（用户口径：等修完再部署）。

## 五、遗留风险（未修，要在 review 里点名）

- 招标抢位（见上，影响评分依据正确性——首块是招标规定而非投标应答）。
- 续接边界对未知编号风格静默失效 → 证据变薄无痕迹（AC2「无静默路径」精神上未达）。
- `直播间总体方案设计` 类字面错位已由 S6 回退解决（真实标书验证过），但仅单元级；
  全链路（evidence_context → runner）未对该路径做接线断言。
- 10 分钟耗时目标未实测。

---

## 追加（2026-08-17 晚 · 提示词收敛会话，不改动上文原清单）

**已完成**（详见 `compound/2026-08-17-decision-bid-auditor-skill-absorption.md` 与
`compound/2026-08-17-explore-server-reachability-audit.md`）：

- 外来 bid-document-auditor skill 四点通用经验吸收进 `/tender-evaluate` + `/tender-extract-info`（+11 行）。
- 提示词收敛：tender-eval 6 个 references 删除（5 死副本 + s1-locate 内联进 extract-info），
  `skills/tender-eval/` 仅剩 SKILL.md；test_prompt_budget / test_tender_pending_reason 的
  旧架构文案同步修正；脚手架实测 57,988 → 38,029 字。
- `.claude/worktrees/agent-a5226a52e06204272` 已清（分支已并入 main、无脏改动）。
- server 使用面审计：文件级死代码 0，肥肉在函数级（见 explore 档）。

**排队项（等第四节⑤整体 review 收口后再动，勿混入 aa08c4e..HEAD review 范围）**：

- ⑧ `AUDIT_INSTRUCTIONS` 迁出 `server/audit/runner.py` → `.claude/commands/audit.md`，
  HTTP worker 改走 `command_adapter.run_command_json("audit")`（照 tender 模式；黄区，小设计增量）。
- ⑨ ~~函数级孤儿清理~~ **已完成**（`f7a2be8`：删 11 函数+1 常量+1 级联方法；保留两个封存
  写端）。**用户已拍板（2026-08-17 晚）**：legacy 路由与 distill-memory 读端**都不删**，维持现状。
  余项：仅测试引用的 4 个函数（converted_page_anchor / find_chapters_by_tag /
  get_result_record_by_request_id / load_expense_rules）随 ⑧ 一并评估。
- ⑩ **新发现待拍板**：全仓无 `RequestAuditRecord` 构造点——请求审计留痕**写路径整体缺失**
  （_admin 读端与 CLI 查询在读一张永远空的表）。审计平台的请求留痕是否应恢复，需用户定。
