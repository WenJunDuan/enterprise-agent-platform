sprint: 2026-06-20-backend-hardening
title: 后端加固 — 收口 round4/round5 两轮审查一致认定的基础设施/正确性债
path: System
owner: claude-code
design: design.md
status: done                       # H1-H4 全绿(299 passed/ruff);F5③ + 续项在 backlog
baseline: "287 passed / ruff clean (2026-06-20 sprint 起点)"
review_pass1: "REWORK→fixed(2026-06-20)：M1/F1/F4/E2 must-fix 已修(54455a1),301 green;余项 backlog。见 reviews/pass1.md"

decisions:
  - "四类债按 成本×价值×风险 排序 H1→H4：先摘低垂果实(真伪闸+F3)，async/租户重活靠后"
  - "不改模型/LiteLLM 层(anyrouter 路由另案)；不重开全量二审；不引向量库"
  - "F3 statute 文件在 gitignored knowledge/，属部署侧编辑，验收以 validate-assets 转 ok 为准(非 git diff)"

tasks:
  - id: H1
    title: 真伪闸默认开 + F3 statute 自检
    status: done                  # 真伪闸 ON + F3 rename(option A) → 288 green / ruff / validate-assets ok
    files:
      - server/common/output_contracts.py        # RULE_REF_CHECK 默认 OFF→ON
      - knowledge/tender/statute-evalmethod.rules.json   # 部署侧：category/rule_id 对齐文件名
      - knowledge/tender/statute-regulation.rules.json   # 部署侧：同上
      - tests/test_contract_registry.py           # 默认开行为 + 任何假 ref fixture 修真
    accept: >
      RULE_REF_CHECK 不设时即生效(假规则号 approved 被拒，空集跳过)；validate-assets 对 tender 转 ok；
      全量绿 + ruff clean。注入规则集==knowledge 规则集 已确认。
    sub:
      - "真伪闸：output_contracts.py:41 默认 '' → '1'；本地全量回归审 fixture"
      - "F3：两 statute 文件 category→statute-*、rule_id 前缀→tender_statute-*_(部署侧 knowledge/)"

  - id: H2
    title: F6 retry/delete 原子化（消除并发双执行/双成本）
    status: done                  # try_transition + delete_if_idle 原子 CAS;294 green(含 8-线程单赢家测试)/ruff
    files:
      - server/stores/audit_task_store.py         # 新 try_transition(原子 CAS 状态转移)
      - server/routes/audit.py                    # retry/delete 用 try_transition 替读-判-写
      - tests/test_audit_task_store.py            # 并发 retry 仅一个成功
    accept: "并发两 retry 仅一个 schedule；delete 与 running 竞态被守住"
    depends_on: [H1]

  - id: H3
    title: F4 to_thread + F5 任务生命周期（异步正确性）
    status: done                  # F4 + F5①② done(298 green);F5③ 杀子进程降级→backlog
    files:
      - server/routes/audit_worker.py             # F4:4处 upsert 包 to_thread；F5①:_BACKGROUND_TASKS 引用集；F5②:admission_available
      - server/routes/audit.py                    # submit/retry 加准入闸→503
      - tests/test_audit_worker.py                # F5①② 引用追踪/自清/准入(asyncio.run,无需 pytest-asyncio)
    accept: "孤儿任务不被 GC(引用集+自清测试)；超额准入返 503；F4 同步写经 to_thread 不阻塞 loop"
    backlog: >
      F5③ 超时杀 claude CLI 子进程降级为 backlog：run_agent 经 async-for query() 流，
      wait_for 取消会 aclose 该 async-gen → SDK transport 清理应已杀子进程；但无可用模型
      (anyrouter 503/本地 litellm down)无法集成验证，recover_stale_audit_tasks 已是 DB 层
      孤儿兜底(重启标 failed)。待模型可用后集成验证再决定是否需显式 kill。
    depends_on: [H1]

  - id: H4
    title: F2 directory 模式租户隔离（数据边界）— approach (b) 完全版
    status: done                  # 三路由统一 tenant 子树;299 green(含跨租户拒绝测试)/ruff
    files:
      - server/routes/upload_helpers.py           # validate_directory_case_path(case,tenant) confine data/submissions/<tenant>/;materialize_* 命名空间化 <tenant>/<request_id>;新 tenant_submission_root
      - server/routes/audit.py / ocr.py / tender.py  # 三路由传 tenant(ocr extract 原丢弃 tenant 已补)
      - tests/test_ocr_routes.py / test_tender_routes.py  # 用例置于 tenant 子树 + 新增跨租户拒绝测试
    accept: "directory 模式不可读 data/db、data/sessions、跨租户 submissions(测试证)；upload 写入 data/submissions/<tenant>/<request_id>"
    note: >
      API 行为变更：HTTP directory 模式(audit/ocr/tender)的 directory_path 现须在
      data/submissions/<tenant>/ 下，外部调用方须把目录置于本租户子树；slash 命令
      /tender-evaluate(本地 AI 直读)不受影响。
    depends_on: [H1]

user_decision:
  - "[已定 2026-06-20] F3 → approach A(重命名去 statute- 前缀，rule_id 不动)"
  - "[已定 2026-06-20] H4/F2 → 本 sprint 做，approach (b) 完全版(三路由统一 tenant 子树 + 上传命名空间化)。done"

backlog:
  - "F5③ 超时杀 claude CLI 子进程：待模型可用后集成验证 SDK aclose 是否已清理(见 H3.backlog)"
  - "tool-trace.jsonl 捕获 .env 明文(本会话 5 处命中)：建议 gitignore tool-trace.jsonl 或 hook 脱敏(本会话未提交该文件)"
  - "round5 余项(非本 sprint)：L5 prompt-injection 边界 / L2·F12 case-memory tender 枚举(部署侧 gitignored schema) / rule.schema.json 死域 hr,legal enum"
  - "review pass1 余项：F2 admission 软上限(await-induced 并发竞态,缓冲 50 足够,非硬上限) / F3 delete-retry 409 极窄竞态消息语义 / F5 H1 默认ON正向 pass 路径测试 / F7 upload_helpers E302 空行 / D1 design.md H1 sub-task 文案(写了 approach B,实现以 A 为准)"
