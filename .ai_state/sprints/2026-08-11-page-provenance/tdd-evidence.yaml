# H2 page-provenance · TDD 证据（八字段全非空，逐 AC 真红真绿）
# 基线：main@4d0a54c 全量 `uv run pytest -q` = 33 failed / 1126 passed / 3 skipped（收集 1162）。
# 本环境 33 条既有失败 = 缺 ocr extra（pymupdf/pptx 等）+ AUDIT_DIRECT_CONNECT 未配，
# 与本 sprint 无关；验收口径 NO_NEW_FAILURES（清单存于 /tmp/BASELINE_FAILURES_33.txt，
# 生成命令：uv run pytest -q -p no:randomly 2>&1 | grep -E "^(FAILED|ERROR)" | sort）。

records:
  - ac_id: "AC1b + KD0 锚协议收拢"
    test_files:
      - tests/test_page_anchor_protocol.py
    red_command: "uv run pytest -q -p no:randomly tests/test_page_anchor_protocol.py"
    red_summary: >-
      首跑 collection ImportError: cannot import name 'converted_page_anchor' from
      'server.common.corpus'（锚协议单点尚不存在）。补齐 corpus 侧后再跑：6 failed / 17 passed
      —— is_ocr_text_valid 对「仅含【转换稿第 1 页】锚行」返回 True（Round1-F4 假 ready 直接复现，
      3 例 parametrize 全红）、boq/docstructure/context_slim 三个平行解析点均不识别转换稿锚变体。
    red_observed_at: "2026-08-11T13:05:00Z"
    green_command: "uv run pytest -q -p no:randomly tests/test_page_anchor_protocol.py"
    green_summary: "23 passed（含 AC1b 的 3 例 reject + 1 例 accept；三个解析点收拢后均识别新锚变体）"
    green_observed_at: "2026-08-11T13:14:00Z"
