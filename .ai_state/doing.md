# Doing

- 已完成职责收口：`.claude/` 与 `knowledge/` 仅承载业务内容，Python 只负责 Claude 调用、HTTP/CLI、持久化、诊断和进程管理。
- 已完成结构化输出约束：`server/core.py` 通过 Claude Agent SDK `output_format` 加载 `.claude/contracts/common/audit-result.schema.json`，不再依赖提示词声明 JSON。
- 已完成服务层重构：`server/` 现在稳定分为入口层（`api/core/cli/chat/app_server`）、基础设施层（`platform/`）和仓储层（`stores/`）。
- 已完成本地可追溯存储：请求审计、会话索引、原始事件流、结构化结果、运行时状态统一落在项目根 `logs/`。
- 已完成会话恢复基础能力：支持 `conversation_id`、`resume_session_id`、`fork_from_session_id`，并可通过 Claude SDK 读取 transcript。
- 已完成兼容清理：旧 wrapper、旧 `server/logs` 残留路径、legacy fallback 读取逻辑以及缓存目录已清理。
- 当前主线已明确：先统一路径模型并移除 `batch-audit` 出主线，再把 `/init-rules` 作为 Claude 第一优先业务能力，随后设计“已审核结果 → 结构化业务记忆”的沉淀链路，最后再进入单条审核业务闭环。
- 已完成当前 skill 整理：以 `.ai_state/docs/audit-skills/` 为基准，重写 `.claude/skills/expense-audit` 与相关 `common/system` skills，并将 `knowledge/expense/*.json` 回链到 `knowledge/external/数睿员工手册.pdf` 第六章制度来源。
- 当前进行中：先收口 CLI 的 `.env -> Claude SDK` 运行面，不进入 HTTP serve；重点是 runtime 映射、缺失校验和 `server.cli ask` 的前置可见性。
- 已解除旧阻塞：本机 Python 环境现已具备 `fastapi`、`typer`、`python-dotenv`、`claude-agent-sdk`、`anthropic`，可执行真实 `server.cli ask` / `audit-json` / `distill-memory` 调用验证。
- 已完成 CLI 基础链路验证：`server.cli runtime` 与 `server.cli ask` 已可实际进入 Claude 调用链并产生日志。
- 已完成 `init-rules expense` 稳定化：空 `initialized` 结果会被拒绝；PDF 会先转文本代理；expense 域按 `general/invoice/loan/entertainment/travel/transport` 拆分执行并聚合；最终结果已归档为 `manual_review`，保留成功写入文件与需人工确认项。
- 已移除 `chat` 模式：`server.cli` 不再暴露交互式 chat 子命令，`server/chat.py` 已删除，CLI 与未来服务边界更清晰。
- 已开始把 Python 越界的业务编排回收到 Claude 侧：`server/cli.py` 不再维护 expense/init-rules 的分类 orchestration，相关大文档拆分策略已迁回 `.claude/commands/init-rules.md` 与 `system/rule-init` skill。
- 已建立统一 Claude command 调用适配层：`init-rules` 与 `audit` 现在通过同一套 Python adapter 从 CLI 与 HTTP 两端进入同一个 Claude command，而不是在 Python 两侧重复实现业务能力。
- 已完成 Python 适配层第二轮收口：`api.py` 和 `cli.py` 的重复输出/错误处理模板已被压缩，Python 更接近“统一 Claude 调用适配层 + 两个输出外壳”的结构。
- 已完成 serve 运行面收口：`app_server.py` 已支持本地 HTTP `/health`、`/ready` 探测并暴露 `service_http` 状态；`api.py` 的 `/chat`、`/audit`、`/init-rules` 共享统一 JSON endpoint 模板；README 已补齐服务调用示例。
- 当前进行中：收口审核结果中文展示契约，要求内部保留三态，对外新增 `result/conclusion/explanation`，并将 `manual_review` 固定为“待人工复核”且必须说明无法自动放行的原因。
- 已完成 serve 异步审核链路：`POST /audit/submit` 支持目录模式与上传模式，`GET /audit/tasks/{request_id}` 提供任务状态，`GET /audit/tasks/{request_id}/result` 提供轻量结果。
- 已完成 serve 硬化第一轮：任务状态增加时间线字段与进度文案，`running` 任务在启动时可恢复/超时失败，上传校验、租户隔离、目录白名单、上传失败回滚、submission 清理与 HTTP 集成测试均已补齐。
- 已完成本地压测样例生成：`data/case2` 到 `data/case21` 已按混合策略落盘，覆盖合法材料、缺件、越界、缺字段、重复附件、空文件、伪扩展名、日期异常和脏文本等场景，并补充 `data/scenario-index.json` 作为索引。
- 已完成 README 中文化重写：文档已按“环境填写 → 项目启动 → HTTP API → 审核与压测 → 部署 → 测试 → 目录结构”的顺序重排，并对齐当前 `.env.example`、CLI、`app-server`、Dockerfile、docker-compose 与 `data/case2-case21` 压测样例。
- 已完成前端审核对接文档重写：`.ai_state/docs/前端审核服务对接文档.md` 已改为顺序化说明，先交代前端主流程只需 4 个接口，再解释 token 来源、真实上传方式、状态轮询、轻量结果读取与常见错误处理。
- 已完成真实材料目录样例：已将桌面真实 PDF 复制到 `data/real-case-001/`，并补充 `audit-request.json` 与目录模式提交 payload，便于部署后直接用 API 或 CLI 验证真实目录输入。
- 已完成 Docker Compose 目录结构对齐：`docker-compose.yml` 已移除过时的 `raw_policies` 挂载，保留 `.claude / knowledge / data / logs` 四类持久化卷，并补充容器名与重启策略；README 的部署说明已同步。
- 已完成前端接入手册收口：`.ai_state/docs/前端审核服务对接文档.md` 已删去冗余背景和运维描述，聚焦前端真实接入所需的请求头、FormData 组装、文件字段、示例数据、状态轮询和结果读取。
- 已完成 Docker 端口环境化：`Dockerfile` 与 `docker-compose.yml` 现在都使用 `APP_SERVER_PORT` 启动 `uvicorn`，Compose 端口映射也随 `.env` 中的端口变化，README 已补充说明。
- 已完成 `docker run` 示例收口：README 现在会先从 `.env` 读取 `APP_SERVER_PORT` 再执行端口映射，避免手工把端口写两遍。
- 已完成 Python 平台清理（Tier 1+2）：stores `__init__` 类型注解补齐（`Path`）、默认 `TENANT_KEYS` 启动告警 + `/ready` 新增 `tenant_keys_are_default`、session/request/result store 软容量保护（超限只 warning 不阻断）、prompt 外置到 `server/prompts/` + `validate_prompts()` 启动校验、引入 `server/platform/logging_setup.py` 统一 JSON/kv logging + `contextvars` 传递 `request_id/tenant/session_id`、FastAPI 中间件 `X-Request-ID` 沿用或生成、租户隔离从 `tenant: str | None = None` 收紧为 `tenant: str` 必填、admin-only 读取路径显式分离、新增 `tests/test_tenant_isolation.py` / `test_session_lifecycle.py` / `test_command_adapter.py` / `test_store_capacity.py` / `test_tenant_key_defaults.py`。详见 `.ai_state/reviews/sprint-python-cleanup.md`。
- 已完成 audit 结果 schema 扩展（P0）：`.claude/contracts/common/audit-result.schema.json` 在 properties 下新增 `manual_review_reason`（enum 7 值：`missing_approval / rule_gap / data_conflict / insufficient_evidence / budget_exceeded / invoice_invalid / pre_approval_mismatch`）与 `risk_dimensions`（array of `{name ∈ invoice/amount/approval/budget/anomaly, score 0-10}`），不入 `required` 保持向后兼容；`server/core.py:validate_structured_output_semantics` 对两个新字段加语义校验（manual_review 必填合法 reason、risk score 严格整数 0-10 显式排除 bool）；相关约束现已回收到 `.claude/commands/audit.md`；`README.md` 返回字段清单补两行说明；新增 `tests/test_audit_result_schema.py`（11 用例）。
- 已撤销 audit P1（2026-04-20）：曾尝试新增 `server/audit/` 目录（`__init__ / contracts / intake / extractor / rules_loader / amount_extract / approval_signatures / prompt_builder / orchestrator`）+ `server/prompts/audit_fast.md` + `POST /audit/fast` 端点，意图用 Python 预读材料/规则把 Claude 调用次数从 5 压到 1；被确认违反"Python 只做通信 / 鉴权 / 服务，是 Claude 的基础脚手架"原则（即使是"数据搬运"也算越线），整体删除，`server/api.py` 的 `/audit/fast` + lifespan 钩子、`server/command_adapter.py` 的 `PROMPT_VARIABLES["audit_fast"]` 同步回退。教训已记入 `.ai_state/lessons.md` 末条。
- 已完成 A-001（2026-04-21）：命令单一事实源收口。`server/command_adapter.py` 不再持有 `audit` / `init-rules` 的独立业务 prompt，也不再依赖 `server/prompts/` 模板目录；Python 现在统一生成 slash command 调用字符串，并补了带空格参数的 quoting。`.claude/commands/audit.md` 的历史 `data/claims` 示例已改为当前主线路径；`tests/test_command_adapter.py` 已改为断言 adapter 直接调用 `/audit ...` 与 `/init-rules ...`。全量 `pytest` 通过（28 项），`ruff check server tests` 通过。详见 `.ai_state/reviews/sprint-agent-a001.md`。
- 已完成 A-001 补充收口（2026-04-21）：把 `manual_review_reason` / `risk_dimensions` / “只返回单个 JSON 对象” / “不要输出 JSON 之外文字” / “不要手工写重复结果文件” 等结构化约束补回 `.claude/commands/audit.md`，避免删掉 Python prompt 后 Claude 端提示强度变弱；README 与蓝图文档已统一强调“Python 只运行服务、获取外部输入并提交给 Claude，审核判断由 Claude 完成”。已做真实 smoke test：`uv run python -m server.cli audit-json data/case1` 虽因路径不存在返回 `manual_review`，但可确认读取路径、发现缺失、生成 `manual_review_reason=insufficient_evidence` 和最终结构化结果都发生在 Claude 侧，Python 仅做提交与归档。
- 已完成 A-002（2026-04-21）：补齐 `extractor -> auditor -> reviewer` 中间契约。新增 `.claude/contracts/expense/extract-result.schema.json` 与 `.claude/contracts/expense/review-delta.schema.json`；`expense-extractor` 现在只输出事实性提取结果，不输出 verdict；`expense-auditor` 明确消费 `extract-result` 并输出最终 `audit-result`；`expense-reviewer` 明确消费原始材料 + `extract-result` + 初审 `audit-result`，并输出 `review-delta`。`.claude/CLAUDE.md` 与蓝图已同步。新增 `tests/test_agent_contracts.py`，全量 `pytest` 通过（33 项），`ruff check server tests` 通过。详见 `.ai_state/reviews/sprint-agent-a002.md`。
- 已完成 A-003（2026-04-21）：落地审后业务记忆沉淀层。新增 `knowledge/_schema/case-memory.schema.json`，定义可复用案例/异常/复核记忆资产；新增 `.claude/commands/distill-memory.md` 与 `.claude/skills/system/memory-distill/SKILL.md`，把“已归档结果 -> knowledge/memory/{domain}/” 的沉淀路径交给 Claude 侧；新增 `knowledge/memory/{expense,hr,legal}/` 目录占位，并在 `.claude/CLAUDE.md` 与蓝图中补齐 system 域对 memory distill 的说明。新增 `tests/test_memory_assets.py`，全量 `pytest` 通过（36 项），`ruff check server tests` 通过。详见 `.ai_state/reviews/sprint-agent-a003.md`。
- 已完成 A-004（2026-04-21）：打通首条真实审核闭环。新增 `tests/fixtures/expense/travel-missing-preapproval/` 作为真实目录输入样例；通过 `uv run python -m server.cli audit-json tests/fixtures/expense/travel-missing-preapproval` 实际执行审核，Claude 读取了 fixture、规则 schema 与 `knowledge/expense/{travel,general}.rules.json`，并对 `EXP-TRAVEL-001` 稳定给出围绕“缺失事前申请/事后补提”的 `manual_review` / `rejected` 结论，命中 `expense.travel.015` / `expense.travel.016` / `expense.travel.018` 等规则。随后通过 `uv run python -m server.cli ask "/distill-memory logs/results/by-request/2026/04/21/070a8b03-b0f7-4519-a07e-d7302048c1c3.json expense"` 实际触发记忆沉淀，生成并校验通过 `knowledge/memory/expense/expense.travel.pre-approval-mismatch.manual-review.v1.json`。同时新增 `common-memory-query`，把 memory 作为 audit 主链的辅助证据来源，并补 `tests/test_audit_memory_integration.py` 与 seed memory 校验。全量 `pytest` 通过（41 项），`ruff check server tests` 通过。详见 `.ai_state/reviews/sprint-agent-a004.md`。
- 已完成运行面收口（2026-04-21）：`server.cli serve` 现在会默认读取 `APP_SERVER_HOST` 与 `APP_SERVER_PORT`，与 `app-server`、`README`、`.env` 行为对齐；新增 `tests/test_cli_serve.py` 锁定该行为，并明确 `distill-memory` 不进入 HTTP API surface。
- 已完成 SDK 升级（2026-04-21）：本地环境与 `uv.lock` 已升级到 `claude-agent-sdk 0.1.64`、`anthropic 0.96.0`，`pyproject.toml` 最低版本线已同步抬高。升级后全量 `pytest` 通过（44 项），`ruff check server tests` 通过。
- 已完成 SDK 收口（2026-04-21）：`review-output` hook 已从 `anthropic` 直连改为统一走 `claude-agent-sdk`，并通过 `tests/test_review_output_hook.py` 锁定“无工具、单轮、禁用 hooks 递归”的行为。当前仓库运行时已移除 `anthropic` 直接依赖，仅保留 `claude-agent-sdk 0.1.64`。升级与收口后全量 `pytest` 通过（47 项），`ruff check server tests` 通过。
- 已完成 A-005（2026-04-21 第一阶段）：查询与治理层升级。`request_store` 已改为“JSONL 日志 + SQLite 查询索引”，`session_store` / `result_store` 已统一用 SQLite 查询索引；新增 `memory_store`，从 `knowledge/memory/**/*.json` 建立 `logs/knowledge/memory-index.sqlite3`；API 新增 `/memories` 与 `/memories/{memory_id}`，并为 `/requests`、`/results` 增加 route/status/verdict/manual_review_reason 等过滤参数，`/results/{request_id}` 现在会返回 `linked_memories`。新增 `tests/test_sqlite_stores.py` 与 `tests/test_query_surfaces.py`，全量 `pytest` 通过（57 项），`ruff check server tests` 通过。
- 已完成 A-005（2026-04-21 第二阶段）：补齐 review_delta 查询与资产治理。新增 `review_delta_store`，以 `logs/review-deltas/index.sqlite3` + `logs/review-deltas/by-request/...` 保存复核差异；API 新增 `/review-deltas`、`/review-deltas/{request_id}` 与 `/governance/assets`；CLI 新增 `review-deltas`、`review-delta-detail`、`validate-assets`；新增 `asset_validation`，可校验 `knowledge/*rules.json` 与 `knowledge/memory/**/*.json`。新增 `tests/test_review_delta_store.py` 与 `tests/test_asset_validation.py`，并扩展 `tests/test_query_surfaces.py` / `tests/test_cli_serve.py`。全量 `pytest` 通过（64 项），`ruff check server tests` 通过。
- 已完成 A-006（2026-04-21）：多域协同与二次复核成本治理。`.claude/CLAUDE.md` 与 `.claude/commands/audit.md` 现在显式定义了 reviewer 自动触发条件、HR/Legal 辅助域触发条件，以及“何时才值得做二审 hook”的成本治理规则；`.claude/hooks/review-output.py` 已从“默认全量二审”收紧为只对 `rejected`、`risk_score >= 70`、以及高风险 `manual_review_reason ∈ {data_conflict, pre_approval_mismatch, missing_approval, invoice_invalid}`` 场景触发。新增 `tests/test_orchestration_triggers.py` 与扩展 `tests/test_review_output_hook.py`。全量 `pytest` 通过（67 项），`ruff check server tests` 通过。
- 已完成 A-007（2026-04-21）：项目级整体架构 review + API 返回集优化 + 发布前整理。`server/api.py` 现在为 `/sessions`、`/conversations`、`/requests`、`/results`、`/memories`、`/review-deltas`、`/sessions/{session_id}/messages` 统一返回 `meta(limit/offset/returned/filters)`，并通过统一异常处理补上 `detail + error{code,message,status_code,path,correlation_id}`；`tests/test_query_surfaces.py` 已锁定新契约；README 与前端接入手册同步说明结构化错误返回；`.ai_state/docs/audit-skills/audit_check.py` 的历史 lint 问题已清理，当前 `uv run pytest -q`（69 项）与 `uv run ruff check .` 均通过。详见 `.ai_state/reviews/sprint-agent-a007.md`。
- 当前边界确认：Python 只做通信 / 鉴权 / 服务，任何"发票 / 规则 / 金额 / 签字节点 / 合规 / 风险"词汇都不允许出现在 Python 代码里；审核继续走 `/audit` 和 `/audit/submit` agentic 路径由 Claude 一手完成。未来如要"降低 Claude 调用次数"只能在 prompt 工程、schema 约束、`max_turns` 上做文章。
