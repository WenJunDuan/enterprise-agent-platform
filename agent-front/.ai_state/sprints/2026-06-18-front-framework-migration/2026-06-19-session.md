# 2026-06-19 Frontend Migration Session

## Summary

Today the migrated `agent-front` UI was finalized for the current business
navigation and login model.

## Completed

- Kept the migrated `quantum-front` shell as the active frontend on `main`.
- Switched login behavior to configured PIN -> tenant key mapping:
  - correct PIN enters the app immediately
  - backend request failures are handled by business pages, not login
- Normalized frontend env files to `.env.dev` and `.env.prod`; `.env.local` is
  no longer part of the intended config set.
- Removed old account/password flows and dashboard route/navigation.
- Renamed product display to `晓数智能云平台`.
- Reorganized sidebar into three domain groups:
  - `发票审核` -> `发票审核清单`
  - `OCR 识别` -> `OCR 识别`
  - `合同审查` -> `合同审查清单`
- Added `/contracts` as the contract review entry route and page.
- Restored the right-header layout/theme configuration drawer.
- Simplified frontend copy so users do not see backend/proxy/HTTP terms in
  normal page messages.

## Verification

- `bun run lint` passed.
- `bun run build` passed; only the existing large-chunk warning remains.
- `bun run test` passed: 15 tests.
- Local dev server remained available at `http://127.0.0.1:5173/`.

## Follow-Up

- Contract review currently has an entry page; real submit/list integration is
  pending backend contract review API scope.
- If system management pages need to return to the sidebar, put them under a
  distinct system management group instead of mixing them into business domains.
