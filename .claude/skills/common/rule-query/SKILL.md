---
name: common-rule-query
description: 根据业务域和类别检索并筛选适用规则
---

# 通用规则查询

## 触发条件

当任意业务域需要根据本地制度做判断时使用。

## 使用方法

1. 根据业务域和类别定位对应规则文件。
2. 读取规则列表。
3. 按角色、条件和优先级筛选匹配项。
4. 返回带 `rule_id` 的匹配结果，供后续引用。

## 冲突处理

- `priority` 数字越小优先级越高。
- 同优先级时，`reject` 优先于 `approve`。
