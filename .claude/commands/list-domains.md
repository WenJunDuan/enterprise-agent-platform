---
description: 列出当前仓库中实际已落地的领域目录；如果只有骨架则明确说明
allowed-tools: Read, Glob
---

检查 `.claude/agents/` 与 `knowledge/` 下的一级目录，并基于实际文件返回当前已落地状态。

要求：

1. 只根据当前仓库文件判断，不根据设计文档推断。
2. 如果目录只有空骨架或占位文件，明确返回“尚未落地”。
3. 不创建、不修改任何文件。
